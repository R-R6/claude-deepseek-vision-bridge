{
  "name": "claude-deepseek-vision-bridge",
  "version": "0.2.1",
  "private": true,
  "description": "Local image-to-text bridge for Claude Code with text-only DeepSeek upstreams.",
  "license": "MIT",
  "scripts": {
    "check": "node --check src/vision-client.js && node --check src/vision-bridge.js && node --check src/vision.js && node --check test/bridge-smoke-test.js && node --check test/startup-script-smoke-test.js",
    "test": "node test/bridge-smoke-test.js && node test/startup-script-smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
